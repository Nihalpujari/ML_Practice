i am taking Lufthansa as my company
data collection : DDGs 

• What are the major opportunities for the company?
• What are the biggest risks?
• What are competitors doing?
• Which technologies or trends should management monitor?
• What strategic actions should be prioritized?
• What evidence supports these recommendations?

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
| Phase                           | Notebook / File                      | Status        | Done       | Tools / Libraries used                                                                               | Deliverable                                              |
|---------------------------------|--------------------------------------|---------------|------------|------------------------------------------------------------------------------------------------------|----------------------------------------------------------|
| Setup (Ollama + llama3.1:8b)    | CEO Agent.ipynb (connection test)    | DONE          | 15/06/2026 | Ollama, llama3.1:8b, ollama (python pkg)                                                             | local LLM installed + ollama.chat tested                 |
| Task 1: Data Collection         | Data Collection.ipynb                | DONE          | 15/06/2026 | ddgs (DuckDuckGo search), json, dict-dedup by url, re (cleaning)                                     | DDGS -> URL+text dedup + clean -> lufthansa_data.json (195) |
| Task 2: Knowledge Repository    | Knowledge Repository.ipynb           | DONE          | 16/06/2026 | sentence-transformers (all-MiniLM-L6-v2), chromadb, re, json                                         | clean + embed + ChromaDB store/index + semantic search   |
| Task 3: Processing              | (folded into Knowledge Repository)   | DONE          | 16/06/2026 | re (clean), sentence-transformers (embed)                                                            | clean + embed happen in Task 2                           |
| Retrieval Layer: Hybrid Search  | Knowledge Repository.ipynb           | DONE          | 17/06/2026 | rank_bm25 (BM25Okapi), sklearn (cosine_similarity), numpy                                            | BM25 + dense, normalize + combine, top-k; tested         |
| Task 4: Intelligence Engine     | Strategic Intelligence Engine.ipynb  | DONE          | 17/06/2026 | transformers pipeline: bart-large-mnli (zero-shot) + cardiffnlp roberta (3-class sentiment), Counter | zero-shot category + 3-class sentiment -> labeled JSON   |
| Documentation (draft)           | README.md                            | DRAFTED       | 17/06/2026 | Markdown, Mermaid diagrams                                                                           | architecture + data-flow diagrams, tech stack, decisions |
| Task 5 & 6: CEO Agent + Recs    | CEO Agent.ipynb                      | DONE          | 18/06/2026 | ollama (llama3.1:8b), RAG (Chroma retrieve + prompt + format=json)                                   | structured recs -> recommendations.json (5)              |
| Section 7: CEO Briefing         | CEO Agent.ipynb                      | DONE          | 18/06/2026 | ollama (llama3.1:8b), format=json                                                                    | exec summary -> ceo_briefing.json (3 questions)          |
| Cleaning pass (clean at source) | Data Collection.ipynb (+ rebuilds)   | DONE          | 18/06/2026 | re, dict text-dedup, low-info filter, chromadb rebuild                                               | clean -> 195 docs; rebuilt Chroma/BM25/labels/recs       |
| Dashboard (8 sections)          | app.py (Streamlit)                   | DONE          | 19/06/2026 | streamlit (radio nav, popover, form, components.html), matplotlib, base64, html                     | Overview/Market/Opp/Risk/Trend/Sentiment/Recs/Briefing + live chat |
| Dashboard UI polish (locked)    | app.py                               | DONE          | 19/06/2026 | streamlit + custom CSS/HTML (magazine cover, editorial mastheads, JS live clock)                    | magazine-cover overview + featured-article recs/briefing |
| Dashboard bonus: Version 2      | app.py                               | TODO (bonus)  |    --      | streamlit (hybrid_search + semantic compare)                                                         | semantic vs hybrid comparison panel                      |
| Oral rehearsal                  | (66 saved questions)                 | TODO          |    --      | exam-questions notes                                                                                 | practice concept Q&A + live-coding extend                |
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
STATUS KEY: DONE = finished | NEXT = doing now | TODO = pending | DRAFTED = written, update as I finish

DEADLINE: 22.06.2026 23:59


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

SETUP (done)
- Installed Ollama, pulled llama3.1:8b (the open-source LLM).
- This LLM will be used in Task 5 (the reasoning engine).
- Python `ollama` package installed; ollama.chat(model="llama3.1:8b", messages=[...]) TESTED -> works.
  (chat messages use roles: system = persona/rules, user = my prompt, assistant = model's past replies.)
  [notebook: CEO Agent.ipynb] -> Task 5 connection is ready.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

15/06/2026

task 1 - Live Data Collection  (DONE)
- The system automatically collects information from at least three public sources.
- I used DDGS (DuckDuckGo search) as the tool.
- 4 content origins (tagged with "source"):
    news       -> general Lufthansa news
    reddit     -> Lufthansa site:reddit.com (community / public opinion)
    competitor -> rival airlines (Air France-KLM, IAG, Ryanair, easyJet, Emirates, Turkish)
    company    -> Lufthansa site:lufthansagroup.com (official / primary source)
- 3 independent origins = news + reddit + company (competitor = extra Market content).
- Ran many query angles per origin to reach volume.
- Removed duplicates (dedup by URL using a dict keyed by url).
- Stored everything in a JSON file: lufthansa_data.json  (~200 docs).
- NOTE: DDGS gives short snippets (title + preview + url), not full articles. The "..." = truncated snippet. URL is kept as evidence for Task 6.
- Did NOT use Wikipedia (its library kept failing with a JSON error).

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

16/06/2026

task 2 - Knowledge Repository  (DONE)   [notebook: Knowledge Repository.ipynb]
- Loaded lufthansa_data.json.
- CLEANING (light, on purpose):
    normalize whitespace (re.sub) + strip + drop docs under 30 chars.
    Did NOT do stopword removal / stemming / lowercasing -> transformer embeddings need natural text.
- No chunking -> docs are short snippets, already one unit each (chunking is only for long docs/PDFs).
- Stored in ChromaDB using PersistentClient(path="chroma_db") so it survives across notebooks/dashboard.
- collection.add(documents, metadatas, ids) -> Chroma AUTO-EMBEDS with all-MiniLM-L6-v2 (384 numbers per doc).
- metadata stored = source + url (the evidence).
- Tested semantic search: collection.query(query_texts=[...]) -> returns docs by MEANING, not keywords.
- This is the RETRIEVAL part of my RAG system.

Extra (understanding / visualization):
- An embedding = a vector of numbers that captures meaning (similar meaning -> nearby vectors).
- Matching = nearest-neighbor: distance (L2 / cosine) between query vector and doc vectors (Chroma uses HNSW).
- Tried PCA & t-SNE 2D scatter plots -> learned 2D CANNOT verify retrieval (it loses ~80% of info).
- The honest visualization is a DISTANCE BAR CHART (the real 384-D ranking).

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

17/03/2026

task 3 (Processing) is folded into task 2 (clean + embed happen there).

RETRIEVAL LAYER - Hybrid Search  (DONE)
- WHY: BM25 (keyword) catches exact rare terms (e.g. "A350"); dense (semantic) catches meaning/synonyms.
  Each is blind where the other is strong -> combine them = better retrieval.
- Built BM25 index over the 200 docs (BM25Okapi, texts lowercased + split = tokenization).
- Step 1: bm25_scores  = bm25.get_scores(query tokens)        -> keyword score per doc
- Step 2: dense_scores = cosine_similarity(query_emb, doc_emb) -> semantic score per doc
- Step 3a: normalize both to 0-1 (min-max) because they are on different scales.
- Step 3b: combined = alpha*dense_norm + (1-alpha)*bm25_norm   (alpha = balance knob, 0.5 = equal)
- Step 4: rank by combined score -> np.argsort(combined)[::-1][:k] -> top-k docs.
- TESTED on "Lufthansa A350 fleet order": top result = exact "A350 (On order)" doc (score 1.000). Good.
- Hybrid vs pure semantic were NEARLY IDENTICAL here (only #2/#3 swapped).
  LESSON: hybrid is a SAFETY NET -> it helps mainly when semantic MISSES an exact rare term;
  on easy queries semantic already works so hybrid changes little (and does no harm).
- TODO (small): wrap Steps 1-4 into one reusable function hybrid_search(query, k, alpha) for the dashboard.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

17/03/2026

task 4 - Strategic Intelligence Engine     [notebook: Strategic Intelligence Engine.ipynb]
- GOAL: add INTELLIGENCE to each doc = two labels:
    category  = opportunity / risk / trend            (zero-shot, facebook/bart-large-mnli)
    sentiment = negative / neutral / positive (3-class, cardiffnlp/twitter-roberta-base-sentiment-latest)
- NOTE: started with binary distilbert-sst-2 (News Analyst tool) but it has NO neutral class + was trained on
  movie reviews -> it FORCED neutral listing pages into pos/neg with high (but unreliable) confidence.
  Switched to a 3-CLASS model (negative/neutral/positive) so neutral factual pages are correctly "neutral".
- WHY zero-shot: no labeled training data; define labels at runtime; model judges fit (NLI entailment).
  HOW it scores: each label becomes a hypothesis "this text is about {label}"; model scores how much the
  text (premise) ENTAILS it; scores normalized; highest wins. (bart-large-mnli = trained on NLI.)
- Both tools come from Agents.ipynb: zero-shot = Support Agent (intent), sentiment = News Analyst Agent (Ex.2).
- Framing = an "Analyst Agent" (perceive doc -> reason classify+sentiment -> act tag doc). Feeds the CEO Agent (Task5).
- Ollama is NOT used here -> small fast models for bulk labeling; Ollama (the reasoning LLM) is for Task 5.
- RESULTS (all 200 docs):
    sentiment -> neutral 118, positive 55, negative 27  (big neutral chunk = listing/factual pages, as expected)
    category  -> trend 100, risk 87, opportunity 13     (opportunity rare: explicit opp framing seldom in short snippets)
  Confidence on generic docs is lowish (0.4-0.6) because the 3 labels overlap and snippets are short -> OK,
  category is a ROUGH sort; the Task 5 LLM does the real reasoning.
- SAVED to lufthansa_labeled.json (200 docs, each has category + sentiment).
- KNOWN ISSUE (deferred to cleaning pass before dashboard): 3 TEXT duplicates survived URL-dedup
  (different URLs, same text -> URL-uniqueness != content-uniqueness). Fix = dedup by text + rebuild Chroma/BM25.
- SUBTYPES (opportunity/risk/trend each have examples in the brief): those are EXAMPLES not required sub-labels.
  Keeping 3 main categories (robust, higher confidence). Subtypes = OPTIONAL; can demo live with a 2-level classifier
  (dict category->subtypes, 2nd zero-shot on subtypes[category]). Drawbacks of doing it now: lower accuracy (errors cascade),
  ~2x runtime, more code, not required -> keep as on-demand demo only.
- Reusable wrapper analyst_agent(doc) (perceive->reason->act) available for the dashboard to label new docs on the fly.
- Data note: models are English; my corpus is English (English DDGS queries), so safe. Non-English would be unreliable.
- text[:512] used for sentiment (BERT/RoBERTa 512 token limit).

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

18/06/2026

task 5 & 6 - AI CEO Agent + Evidence-Based Recommendations  (DONE)   [notebook: CEO Agent.ipynb]
- This is the RAG put together (Retrieve -> Augment -> Generate). Ollama llama3.1:8b is the reasoning engine.
- ceo_agent(question, k=5):
    1. RETRIEVE: collection.query(query_texts=[question], n_results=k) -> top-k docs (semantic)
    2. CONTEXT: join docs, each tagged "[source] text" so the LLM sees provenance
    3. GENERATE: ollama.chat(model="llama3.1:8b", messages=[system, user], format="json")
       - system prompt = CEO-advisor persona + "use ONLY the evidence" (anti-hallucination) + exact JSON keys
       - format="json" forces valid JSON (but NOT the shape -> must spell out keys in the prompt)
    4. PARSE: json.loads(...); attach question + source URLs (the clickable evidence)
- JSON keys per recommendation: recommendation, justification (covers Task5 "justify"), supporting_evidence,
  expected_impact, risk_level (High/Med/Low), priority (High/Med/Low). + question + sources(urls).
- Maps to brief: Task6 (rec/evidence/impact/risk) + dashboard Sec6 (rec/priority/evidence/impact/risk) + Task5 "justify".
- PRE-GENERATED 5 CEO questions -> recommendations.json  (slow: ~2.5 min/call on CPU; run ONCE, dashboard loads file).
- Reddit IS real: ceo_agent sources included reddit.com URLs -> partially answers my "is reddit data real?" question.

Section 7 - CEO Briefing  (DONE)   [notebook: CEO Agent.ipynb]
- ceo_briefing(recommendations): one LLM call that SYNTHESIZES the recommendations into an exec summary.
- JSON keys = the brief's 3 questions: what_happened / why_it_matters / what_to_do_next (plain strings).
- NOTE: first run returned NESTED dicts -> had to add "each value MUST be a plain string" to the prompt.
  LESSON: format="json" guarantees valid JSON, NOT the exact shape -> control shape via explicit prompt.
- SAVED to ceo_briefing.json.
- WHY pre-generate everything (recs + briefing) to JSON: 8B on CPU is slow; dashboard loads files = fast + reliable.
  For the demo: run from saved artifacts (consistent) + run small live samples (1 ddgs query, 1 ceo_agent call) to prove it's real.
  DON'T re-run only collection mid-demo -> new live data desyncs Chroma/labels/recs. All-fresh OR all-saved, never mixed.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

18/06/2026

CLEANING PASS  (DONE)  -> moved ALL cleaning into Data Collection.ipynb (clean at the SOURCE)
- WHY here: clean once at collection -> lufthansa_data.json is already clean -> every downstream stage uses clean data
  (also fixes the old inconsistency where Task4 read raw text but Chroma held cleaned text).
- Cleaning now applied (after URL-dedup, before save):
    whitespace normalize + collapse "...." + drop <30 chars + drop low-info/navigation pages (<10 unique words) + TEXT-dedup.
- Result: 202 -> 195 clean docs (removed 7: dupes + menu/listing pages).
- REBUILT everything on the 195 so all artifacts align:
    Chroma (delete-if-exists then re-add), BM25, doc_emb, relabeled lufthansa_labeled.json, regenerated recommendations.json + ceo_briefing.json.
- Chroma delete made idempotent: if any(c.name=="lufthansa" for c in client.list_collections()): client.delete_collection(...)
- Knowledge Repository's own cleaning cells are now redundant -> kept `cleaned_docs = documents` alias so downstream code still runs.
- LESSON: URL-uniqueness != content-uniqueness (3 text dupes survived URL-dedup); dedup by text too. Clean NOISE not MEANING.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

WHOLE SYSTEM = a RAG pipeline:
  collect (Task1) -> store/index (Task2) -> retrieve (semantic + hybrid) -> generate with Ollama (Task5) -> dashboard
  RAG = Retrieval + Augmented(prompt) + Generation. BM25 is just an algorithm inside the Retrieval stage.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

19/06/2026

DASHBOARD  (DONE + UI LOCKED)   [file: app.py, Streamlit]
- Single file, flat folder, multi-page via sidebar st.radio. Loads SAVED artifacts (fast); LLM only called live in the chat.
- 8 pages: Overview / Market Intelligence / Opportunities / Risks / TRENDS (new) / Sentiment / Recommendations / CEO Briefing.
- Caching: @st.cache_data (JSON data) vs @st.cache_resource (Chroma collection + embedding model = heavy objects).
- LIVE CHAT (replaces a separate "Ask" page): a floating round button bottom-right via st.popover + CSS (position:fixed).
    Click -> popup with chat history + st.form(clear_on_submit) input -> calls ceo_agent(question) LIVE (only live-Ollama spot).
    st.form is used so a keystroke doesn't rerun/close the popover; submit clears the box. History kept in st.session_state.
- MARKET INTELLIGENCE: 4 source tabs (News/Reddit/Competitor/Company) showing FULL text (removed the old [:220] truncation).
- FILTERS (deliberate design choice for the oral):
    Opportunities + Trends -> filter by SENTIMENT (positive/neutral/negative is meaningful there).
    Risks -> filter by SOURCE (news/competitor/reddit/company). WHY: category & sentiment are INDEPENDENT labels, so
    "positive risk" is nonsense; the useful question for a risk is "where is it coming from?" not "what's its tone?".
- SENTIMENT page: matplotlib pie given a navy facecolor + white label/percent text so it's readable on ANY theme.
- VISUAL/UI (locked 19/06):
    * Magazine-cover Overview: full-bleed background image (base64), masthead "AI CEO / THE LUFTHANSA EDITION",
      editorial cover-lines teasing the live opp/risk/trend counts, VOL.1 issue line + footer stats, "FROM THE EDITOR"
      About+Why in serif w/ drop-cap.
    * Editorial hero() masthead on every content page (kicker + yellow rule + big title) = magazine running header.
    * Recommendations = "featured article" cards: numbered badge + left border tinted by PRIORITY (red/orange/green),
      serif headline, italic pull-quote justification, evidence list, sources expander.
    * CEO Briefing = editorial article: drop-cap lead, serif justified body, yellow kicker section heads.
    * Brand CSS: navy #05164D + yellow #F9BA00 sidebar, category colour accents (opp green/risk red/trend blue).
    * LIVE CLOCK in sidebar via st.components.v1.html (JS setInterval) -- a clock can't be done in pure Streamlit
      because reruns are EVENT-DRIVEN (only on interaction), not a continuous loop; JS ticks in the browser instead.
      (st.markdown CANNOT run <script> -- Streamlit strips it; only components.html executes JS, inside an iframe.)
    * All LLM-generated text is html.escape()'d before going into HTML cards so a stray < or & can't break the layout.
- DOM note: verified test-ids against the installed Streamlit 1.37 JS bundle -> metric container is stMetric (NOT
  "metric-container"); popover = stPopover / stPopoverButton / stPopoverBody; widget wrapper = element-container.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

20-21/06/2026

PROF FEEDBACK (Mobasher) + ENVIRONMENT FIX + RETRIEVAL EVALUATION

PROF FEEDBACK (assistant examiner Mobasher; MAIN module leader = Prof SWATI -> follow task instructions + get HER final feedback):
- Label the diagram ARROWS, especially those into Task 3 (processing) & Task 4 (intelligence) -> DONE (README mermaid arrows labelled).
- Be ready to EXPLAIN the whole architecture orally + JUSTIFY every design choice ("even if you used AI tools, you must understand everything").
- He asked: "for hybrid, what is the criteria of the MAJORITY VOTING of such ensemble methods?" -> answer: my hybrid is NOT majority voting (that's a CLASSIFICATION strategy - discrete votes). It's WEIGHTED SCORE FUSION: min-max normalise BM25 + cosine to 0-1, weighted sum (alpha), take top-k. RRF = the rank-voting alternative.
- "No limit on question types" but "it is not a master thesis defence" -> focus = understand the model + justify decisions, not exotic depth.

ENVIRONMENT / HOW TO RUN THE DASHBOARD (fixed):
- The full ML stack (chromadb 1.5.9, sentence-transformers, ollama, sklearn, ...) lives in PYTHON 3.11 (C:\Users\hp\AppData\Local\Programs\Python\Python311). chroma_db was BUILT by this chromadb.
- PATH `streamlit`/`jupyter` point to Python 3.12, which has streamlit but NO chromadb -> `streamlit run app.py` failed with ModuleNotFoundError: chromadb.
- FIX: installed streamlit==1.37.0 into 3.11. ALWAYS launch with explicit 3.11:
    & "C:\Users\hp\AppData\Local\Programs\Python\Python311\python.exe" -m streamlit run app.py     (NOT plain `streamlit run app.py`)

RETRIEVAL EVALUATION - semantic vs hybrid vs BM25 (answers "why semantic and not hybrid?"):
- Tested THREE ways on the 5 CEO questions (semantic = collection.query; hybrid = BM25+cosine fused; BM25 = keyword only):
  (1) OVERLAP of top-5 docs: semantic∩hybrid ~1.4/5 (alpha=0.5), 2.4/5 (alpha=0.7); semantic∩BM25 only 0.4/5 -> they retrieve DIFFERENT evidence.
  (2) ALPHA SWEEP: overlap-with-semantic rose monotonically 0.4 -> 5.0 as alpha 0->1 (alpha = the dial between BM25 and semantic). BM25 "punches above its weight" (alpha=0.7 still only 2.4/5; need ~0.9) because min-max norm makes BM25 spiky vs cosine bunched.
  (3) RELEVANCE by Task-4 label (does the top-5 doc's category/source match the question intent): semantic 6, hybrid 7, BM25 5 of 20 -> COMPARABLE, no clear winner, question-dependent (BM25 best on risks; sem/hyb best on trends; competitors 0/0/0 = proxy artifact).
  (4) END-TO-END (the decider): ran the FULL LLM agent on "biggest risks" with semantic vs hybrid retrieval -> SAME recommendation (cost management + pricing strategy, High risk, both cite the 37% profit drop). Output CONVERGES even though the docs differ, because the corpus's dominant theme (cost/competition pressure) sits in any reasonable top-5.
- CONCLUSION: output is the same -> quality is a TIE -> decide on COST. Semantic = ONE ChromaDB call, no extra keyword index, no alpha to justify; hybrid adds complexity for an IDENTICAL result. So the AGENT USES SEMANTIC; hybrid kept as INSURANCE (if data becomes keyword-heavy / full articles -> raise alpha or wire it in, a 1-line swap). Short DDGS snippets also favour semantic. Fully adaptive QUERY ROUTING = future work.
- Documented as README Design Decision #17. Full oral answers saved in exam-questions Q72-Q74.
- LESSON: MEASURE, don't assume. My earlier "semantic is clearly better" was OVERSTATED - the objective test showed them comparable -> present the honest nuance (examiners reward self-correction over false confidence).

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

=================== NEXT STEPS ===================
- Dashboard DONE + UI LOCKED (19/06). README updated incl. Design Decision #17. Launch via Python 3.11 (see ENVIRONMENT FIX above), needs Ollama for the live chat.
- Option B (wire hybrid into the agent): RESOLVED - evaluated 3 ways, semantic chosen by COST since output converges; hybrid kept as insurance. NOT wiring it in.
- BREADTH ORAL REHEARSAL (highest priority; deadline 22/06): retrieval is now bulletproof; STILL TO DRILL -> Task 4 (3-class sentiment + zero-shot NLI), ChromaDB choice, cleaning philosophy ("remove noise not meaning"), format=json (validity vs schema), full RAG flow.
- SUBMIT EARLY 22/06 (do NOT leave it to 23:58).
- Dashboard bonus (optional): Version 2 = semantic vs hybrid comparison panel.

DATA FILES (artifacts the dashboard reads) — all now consistent at 195:
  lufthansa_data.json (195, clean, Task1) -> chroma_db/ + BM25 (Task2) -> lufthansa_labeled.json (195, Task4) ->
  recommendations.json (5, Task5/6) + ceo_briefing.json (Sec7)

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------