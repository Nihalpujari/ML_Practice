# 📈 Basic Regression *(supervised mirror)*

This folder mirrors the top-level [`basic regression/`](../../basic%20regression/) folder so the supervised bundle is self-contained.

➡️ **Full walkthrough:** [../../basic regression/README.md](../../basic%20regression/README.md)

---

## 📂 Subfolders

| Folder | Algorithm |
|--------|-----------|
| `diabetes/` | Linear Regression |
| `redge reg/` | Ridge Regression (L2) |
| `lasso reg/` | Lasso Regression (L1) |
| `logistic reg/` | Logistic Regression (classification) |

Plus the standalone `visual CV Lreg.py` — visualize 6-fold cross-validation scores with mean and 95% confidence interval.

### Bonus in this mirror
The `diabetes/` folder here contains an extra notebook, `prg2.ipynb`, with additional experiments on top of the canonical `code.ipynb`.
