# 🧹 Data Preprocessing *(supervised mirror)*

This folder is a **mirror copy** of the top-level [`data preprocessing/`](../../data%20preprocessing/) folder, kept inside `supervised/` so the supervised learning bundle is self-contained.

For the full walkthrough — categorical encoding, the Titanic CSVs, the GitHub-candidates dataset and tips — see the canonical README:

➡️ **[../../data preprocessing/README.md](../../data%20preprocessing/README.md)**

---

## 📂 Contents

| File | Purpose |
|------|---------|
| `categorical features.ipynb` | Label & one-hot encoding of categorical columns |
| `train.csv`, `test.csv` | Titanic training & test sets |
| `github_candidates_1.csv` | Small GitHub-profiles dataset |
